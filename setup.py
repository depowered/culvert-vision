from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A computer vision model for detecting culverts in high-resolution digital elevation models.',
    author='depowered',
    license='MIT',
)
